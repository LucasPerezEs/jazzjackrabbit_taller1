#ifndef TEXTURE_IDS
#define TEXTURE_IDS

enum EntityType {
    JAZZ = 0x00,
    LORI = 0x01,
    SPAZ = 0x02
    // ENEMIGOS
};

enum AnimationType {
    IDLE = 0x10,
    WALK = 0x11,
    RUN = 0x12,
    JUMP = 0x13,
    FALL = 0x14,
    RECIEVE_DAMAGE = 0x15,
    DEAD = 0x16,
    SPECIAL_ACTION = 0x17
};

#endif
